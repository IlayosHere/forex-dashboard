# -----------------------------------------------------------------------
# Consolidated Python requirements for api/ and runner/
# Both Dockerfiles install from this single file.
# -----------------------------------------------------------------------

# Web framework (api)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database (api + runner)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Validation (api)
pydantic>=2.0.0

# Authentication (api)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Config / env
python-dotenv>=1.0.0

# HTTP (discord notifier in impulse-notifier/)
requests>=2.32.3

# Market data + strategies
pandas==2.2.3
tvDatafeed @ git+https://github.com/rongardF/tvdatafeed.git

# Analytics engine
scipy>=1.13.0
numpy>=1.26.0
