# -----------------------------------------------------------------------
# Python requirements for api/
# -----------------------------------------------------------------------

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Validation
pydantic>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt must stay <4.1 because passlib 1.7.4 reads bcrypt.__about__
# which was removed upstream in 4.1. Without this pin, login 500s.
bcrypt<4.1

# Config / env
python-dotenv>=1.0.0

# Market holiday calendars (forex bank holidays — offline, no network dependency)
holidays>=0.5
